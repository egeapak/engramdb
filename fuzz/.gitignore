target/
artifacts/
coverage/

# Auto-grown fuzzer corpus state — keep only curated seeds (.md for the
# memory-file targets, .json for the registry target).
corpus/*/*
!corpus/*/*.md
!corpus/*/*.json
